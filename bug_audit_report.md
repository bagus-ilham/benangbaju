# 🔍 Bug Audit Report — Benangbaju (Deep Audit Phase 35)

**Project**: Benangbaju E-commerce  
**Framework**: Next.js 16.2.9 (App Router) · React 19.2.4 · TypeScript  
**Payment Gateway**: DOKU (Jokul Checkout) *(Midtrans Fully Deprecated)*  
**Database/Auth**: Supabase (PostgreSQL + Auth + Edge Functions)  
**State Management**: Zustand · TanStack Query  
**Audit Date**: 2026-07-31 (Re-audit Phase 35)  

---

## Ringkasan Temuan

| Severity | Jumlah |
|----------|--------|
| 🔴 Critical | 12 |
| 🟠 High | 29 |
| 🟡 Medium | 44 |
| 🟢 Low | 5 |
| **Total** | **90** |

---

## Detail Temuan

| # | File:Baris | Severity | Kategori | Deskripsi | Dampak | Saran Perbaikan |
|---|-----------|----------|----------|-----------|--------|----------------|
| 1 | [auth-guard.ts:4-28](file:///d:/Aulia%20Project/benangbaju/src/lib/auth-guard.ts#L4-L28) | 🔴 Critical | Keamanan / Auth Guard | **`requireAuth` dan `requireAdmin` tidak memverifikasi flag `is_active`.** Fungsi guard hanya mengeksekusi `supabase.auth.getUser()` dan cek role `admin`, tanpa memeriksa apakah `profiles.is_active` bernilai `true`. | **Pengguna atau staf yang telah di-nonaktifkan/di-ban (status `is_active = false`) tetap bisa mengeksekusi Server Actions** (checkout, cancel order, update data admin, dll). | Tambahkan pengecekan `if (!profile || !profile.is_active) throw new ForbiddenError('Account is disabled')` di `requireAuth` dan `requireAdmin`. |
| 2 | [doku-webhook/index.ts:35-41](file:///d:/Aulia%20Project/supabase/functions/doku-webhook/index.ts#L35-L41) | 🔴 Critical | Keamanan / DOKU | **`constantTimeCompare` bocor panjang string.** Fungsi mengembalikan `false` langsung di baris 36 jika `a.length !== b.length`. Ini membuat attacker bisa mengetahui panjang signature yang benar lewat response-time perbedaan. | Signature verification bisa di-bypass secara bertahap melalui timing attack. | Pad kedua string ke panjang yang sama (misal hash keduanya dulu dengan SHA-256 seperti pada `proxy.ts:48-60`) sebelum membandingkan byte-per-byte. |
| 3 | [create_order SQL:182](file:///d:/Aulia%20Project/supabase/migrations/20260622132600_fix_create_order_fk_violation.sql#L182) & [generate-payment:116](file:///d:/Aulia%20Project/supabase/functions/generate-payment/index.ts#L116) | 🔴 Critical | DOKU / Root Cause | **Double Insert Payment Record (Root Cause).** RPC `create_order` memasukkan row `payments` pertama (`status: 'pending'`) saat checkout. Kemudian Edge Function `generate-payment` me-insert row `payments` kedua tanpa mengecek record eksisting. | **Setiap 1 pesanan selalu memiliki 2 (atau lebih) record pembayaran duplikat** di tabel `payments`, memicu kebingungan webhook & reporting. | Hapus insert `payments` di RPC `create_order` ATAU ubah `generate-payment` agar meng-update record `payments` yang dibuat oleh `create_order` alih-alih me-insert row baru. |
| 4 | [generate-payment/index.ts:116-128](file:///d:/Aulia%20Project/supabase/functions/generate-payment/index.ts#L116-L128) | 🔴 Critical | DOKU / Data Integrity | **Duplikat payment record saat retry.** Setiap kali `generate-payment` dipanggil (termasuk retry dari `invokeWithRetry`), me-insert ke tabel `payments` tanpa cek apakah payment record untuk `order.id` sudah ada. | Duplikat data di tabel `payments`; webhook idempotency check bisa salah match karena multiple payment records. | Gunakan `upsert` dengan `onConflict: 'order_id'`, atau cek `SELECT` dulu sebelum me-insert. |
| 5 | [doku-webhook/index.ts:274-282](file:///d:/Aulia%20Project/supabase/functions/doku-webhook/index.ts#L274-L282) | 🔴 Critical | DOKU / Idempotency | **Payment log tidak terinsert jika `payment` record tidak ada.** Log idempotency hanya ditulis jika `payment?.id` truthy (L275). Jika payment record gagal dibuat atau belum ada, `payment_logs` tidak tertulis. | Webhook berikutnya untuk event yang sama akan diproses ulang (order status flip-flop, notifikasi ganda). | Tulis `payment_logs` tanpa bergantung pada `payment.id` — gunakan `payment_id: payment?.id ?? null`. |
| 6 | [generate-payment/index.ts:59](file:///d:/Aulia%20Project/supabase/functions/generate-payment/index.ts#L59) | 🔴 Critical | DOKU / Konfigurasi | **DOKU endpoint hardcoded ke sandbox.** `dokuEndpoint` di-hardcode `'https://api-sandbox.doku.com'` tanpa baca dari env var (juga di `check-payment-status/index.ts:58`). | **Transaksi real tidak akan pernah berhasil di production.** | Pindahkan ke env var `DOKU_API_URL` atau derive dari `DOKU_ENVIRONMENT`. |
| 7 | [[slug]/page.tsx:56,64](file:///d:/Aulia%20Project/benangbaju/src/app/%28customer%29/%5Bslug%5D/page.tsx#L56) | 🔴 Critical | Keamanan / XSS | **`dangerouslySetInnerHTML` tanpa sanitasi pada konten CMS.** Konten JSONB dari tabel `landing_pages` langsung dirender sebagai raw HTML. | Stored XSS — attacker bisa mencuri session cookies atau melakukan tindakan atas nama user. | Sanitasi HTML menggunakan `isomorphic-dompurify` sebelum merender. |
| 8 | [cart.repository.ts:101-112](file:///d:/Aulia%20Project/benangbaju/src/modules/cart/cart.repository.ts#L101-L112) | 🔴 Critical | Data Loss / DB | **Fallback `replaceItems` menghapus seluruh cart items sebelum insert.** Di fallback method `replaceItems`, `cart_items` di-delete lebih dulu dengan `.delete().eq('cart_id', cartId)`. Jika `.insert(insertData)` di L110 gagal (misal karena constraint error), data keranjang terhapus total. | **Keranjang pengguna hilang permanen** jika terjadi error saat memasukkan item baru. | Gunakan database transaction atau jalankan insert dalam try-catch, jika insert gagal kembalikan item lama atau atur rollback. |
| 9 | [cart.mapper.ts:21-23](file:///d:/Aulia%20Project/benangbaju/src/modules/cart/cart.mapper.ts#L21-L23) | 🔴 Critical | Data Integrity / Cart | **Pengecekan harga `typeof pv.price === 'number'` gagal untuk Postgres numeric.** Pada `mapDbCartItemToLocal`, jika Supabase SDK mengembalikan nilai kolom Postgres `numeric` sebagai string (`"150000"`), pengecekan `typeof === 'number'` gagal dan `pvPrice` bernilai 0. | **Harga item di keranjang berubah menjadi Rp 0**, berpotensi menyebabkan transaksi Rp 0 di checkout. | Gunakan konversi eksplisit: `const pvPrice = Number(pv?.price) \|\| 0`. |
| 10 | [product.mapper.ts:61-85](file:///d:/Aulia%20Project/benangbaju/src/modules/products/product.mapper.ts#L61-L85) | 🔴 Critical | Display / Catalog | **Produk tanpa varian aktif menampilkan harga Rp 0.** Jika produk tidak memiliki varian aktif (`is_active: false` untuk semua variant), `activeVariantsCount` bernilai 0 dan `minPrice` tidak di-assign, me-return `minPrice = 0`. | **Produk di katalog/homepage tampil dengan harga Rp 0** pada card produk. | Set `minPrice` dari varian non-aktif jika tidak ada varian aktif, atau berikan flag `isOutOfStock` dan sembunyikan harga Rp 0. |
| 11 | [koleksi/[slug]/page.tsx:18-29](file:///d:/Aulia%20Project/benangbaju/src/app/%28customer%29/koleksi/%5Bslug%5D/page.tsx#L18-L29) | 🔴 Critical | Caching / SSR Error Vector | **Vektor Error Caching Exception pada Halaman Detail Koleksi.** `getCachedCollection` me-throw `new Error('Collection ... not found')` di dalam konteks `'use cache'`. Di Next.js 16, melempar exception di dalam cached function membekukan exception di cache engine. | Pengunjung yang mengakses URL koleksi tidak ada memicu **500 Server Exception** yang membeku di cache alih-alih merender `notFound()` 404. | Pindahkan validasi ketiadaan data ke luar fungsi cached atau kembalikan `null` lalu panggil `notFound()` di komponen utama. |
| 12 | [home.service.ts:15-41](file:///d:/Aulia%20Project/benangbaju/src/modules/home/home.service.ts#L15-L41) | 🔴 Critical | Caching / Logic | **`'use cache'` membekukan timestamp `now` pada Flash Sale beranda.** `getCachedHomepageData` meng-cache data homepage selama berjam-jam. Filter tanggal `lte('starts_at', now)` membeku pada timestamp saat cache dibuat. | Flash sale yang baru dimulai atau sudah berakhir **tetap tertahan di tampilan beranda** tanpa terupdate hingga cache expire. | Jangan sertakan query waktu sensitif seperti `getActiveFlashSale` di dalam fungsi cache jam-jaman. |
| 13 | [settings.repository.ts:58-66](file:///d:/Aulia%20Project/benangbaju/src/modules/settings/settings.repository.ts#L58-L66) | 🔴 Critical | Admin / Data Loss | **`adminUpdateSettings` membuang setting key baru.** `adminUpdateSettings` memfilter array input terhadap `currentSettings` yang diambil dari database. Jika ada key baru di form yang belum ada barisnya di DB, filter di L59 membuang key tersebut. | Admin tidak dapat menambah atau menyimpan pengaturan situs baru dari dashboard admin. | Gunakan `adminUpsertSettings` yang langsung meng-upsert key & value tanpa mem-filter terhadap data DB eksisting. |
| 14 | [useFocusTrap.ts:3-44](file:///d:/Aulia%20Project/benangbaju/src/shared/hooks/useFocusTrap.ts#L3-L44) | 🟠 High | UI State / UX Lock | **Kebocoran Lock Scroll `document.body` (Halaman Web Macet/Tidak Bisa Di-scroll).** Variabel global `activeTrapCount` dan manipulasi `overflow: hidden` pada `useFocusTrap` tidak memulihkan `style.overflow` saat modal/dialog di-unmount secara tak terduga (misal lewat navigasi halaman). | **Seluruh halaman aplikasi web macet total (tidak bisa di-scroll)** oleh pelanggan atau admin setelah membuka dan menutup modal. | Gunakan React Context / State Manager khusus untuk scroll lock dan pastikan cleanup function selalu meriset `document.body.style.overflow = ''`. |
| 15 | [category.repository.ts:173-183](file:///d:/Aulia%20Project/benangbaju/src/modules/categories/category.repository.ts#L173-L183) | 🟠 High | Data Integrity / Storage | **Hapus gambar kategori dari storage dilakukan sebelum hapus record DB.** `adminDeleteCategory` menghapus gambar kategori di Storage sebelum mengeksekusi `.delete().eq('id', categoryId)` di DB. Jika DB delete gagal, gambar terhapus fisik sementara record masih ada di DB. | Gambar kategori terhapus permanen dari storage sementara record kategori masih ada di DB (broken image). | Lakukan DB delete terlebih dahulu; hapus fisik gambar di Storage hanya jika DB delete berhasil. |
| 16 | [users/actions.ts:70-73](file:///d:/Aulia%20Project/benangbaju/src/modules/users/actions.ts#L70-L73) | 🟠 High | Keamanan / Self-Lockout | **Kerentanan Self-Deletion Admin pada penghapusan staf.** `deleteAdminStaffAction` tidak mengecek apakah `staffId === user.id`. | Seorang admin dapat **tidak sengaja menghapus akun admin miliknya sendiri** lewat dashboard staf, terkunci dari sistem secara permanen. | Tambahkan proteksi: `if (staffId === user.id) throw new ForbiddenError('Tidak dapat menghapus akun sendiri')`. |
| 17 | [order.repository.ts:210-212](file:///d:/Aulia%20Project/benangbaju/src/modules/orders/order.repository.ts#L210-L212) | 🟠 High | Admin / Order Flow | **Pembatalan pesanan admin terblokir untuk status `processing`/`shipped`.** `adminUpdateStatus` memanggil RPC `cancel_order`, tetapi RPC `cancel_order` memvalidasi `v_order.status == 'pending_payment'`. | **Admin tidak dapat membatalkan pesanan berstatus `processing` atau `shipped`** melalui dashboard admin. | Modifikasi RPC `cancel_order` atau buat RPC khusus `admin_cancel_order` yang mengizinkan pembatalan pesanan diproses. |
| 18 | [inventory.repository.ts:69-81](file:///d:/Aulia%20Project/benangbaju/src/modules/products/inventory.repository.ts#L69-L81) | 🟠 High | Performa / Timeout Vector | **Sequential DB queries loop pada sync stok massal ERP.** `bulkUpdateStock` mengeksekusi query `SELECT` dan dispatch notifikasi berurutan per-SKU di dalam `for loop`. Pada sync 100+ SKU, fungsi mengeksekusi ratusan query DB berurutan. | **API `/api/v1/inventory/sync` melempar 504 Gateway Timeout** / Vercel Serverless Function Limit Exceeded pada sync ERP massal. | Lakukan bulk query `in('sku', skus)` dan bungkus dispatch notifikasi sebagai background job/queue. |
| 19 | [collection.repository.ts:117-128](file:///d:/Aulia%20Project/benangbaju/src/modules/collections/collection.repository.ts#L117-L128) | 🟠 High | Data Integrity / DB | **Pembuatan koleksi non-transaksional meninggalkan koleksi kosong saat error.** `adminCreateCollection` me-insert parent `collections` terlebih dahulu. Jika insert ke `collection_products` gagal di L123, fungsi me-return fail tetapi row parent `collections` terlanjur tersimpan di DB. | Koleksi kosong tanpa produk terbuat di DB saat terjadi kesalahan asosiasi produk. | Gunakan RPC Postgres (transaction) atau rollback delete `collections` jika insert junction gagal. |
| 20 | [collection.repository.ts:213-219](file:///d:/Aulia%20Project/benangbaju/src/modules/collections/collection.repository.ts#L213-L219) | 🟠 High | Data Integrity / Storage | **Hapus gambar koleksi dari storage dilakukan sebelum hapus record DB.** `adminDeleteCollection` menghapus gambar koleksi di Storage sebelum mengeksekusi `.delete().eq('id', collectionId)` di DB. Jika DB delete gagal, gambar terhapus fisik sementara record masih ada di DB. | Gambar koleksi terhapus permanen dari storage sementara record koleksi masih ada di DB (broken image). | Lakukan DB delete terlebih dahulu; hapus fisik gambar di Storage hanya jika DB delete berhasil. |
| 21 | [banner.repository.ts:146-160](file:///d:/Aulia%20Project/benangbaju/src/modules/banners/banner.repository.ts#L146-L160) | 🟠 High | Data Integrity / Storage | **Hapus gambar banner dari storage dilakukan sebelum hapus record DB.** `adminDeleteBanner` menghapus gambar banner di Storage sebelum mengeksekusi `.delete().eq('id', bannerId)` di DB. Jika DB delete gagal, gambar terhapus fisik sementara record banner masih ada di DB. | Gambar banner terhapus permanen dari storage sementara record banner masih ada di DB (broken image). | Lakukan DB delete terlebih dahulu; hapus fisik gambar di Storage hanya jika DB delete berhasil. |
| 22 | [product.repository.ts:77](file:///d:/Aulia%20Project/benangbaju/src/modules/products/product.repository.ts#L77) | 🟠 High | DB / Syntax Error | **Sintaks pencarian `query.or()` rusak saat query mengandung koma.** `findMany` menggunakan `query.or(\`name.ilike.%${escapedSearch}%,description.ilike.%${escapedSearch}%\`)`. Koma di dalam kata kunci (misal `"Kaos, Pria"`) memutus sintaks PostgREST `.or()`. | Pencarian produk di katalog melempar PostgREST syntax error `PGRST100`. | Escape karakter koma `,` atau gunakan full-text search / RPC pencarian produk. |
| 23 | [review.repository.ts:319-330](file:///d:/Aulia%20Project/benangbaju/src/modules/reviews/review.repository.ts#L319-L330) | 🟠 High | Concurrency / Abuse | **Read-modify-write race condition pada fallback `voteHelpful`.** Fallback `voteHelpful` membaca counter, menambahkan 1, lalu melakukan update. Panggilan bersamaan mengalami Lost Updates. Selain itu tidak ada cek `userId`/IP untuk mencegah spam upvote. | Jumlah vote helpful tidak akurat saat dipanggil paralel, dan pengguna/bot dapat melakukan vote ulang tanpa batas. | Wajibkan penggunaan RPC atomic `increment_review_helpful` dan simpan tabel `review_votes` berbasis `user_id`. |
| 24 | [order.repository.ts:263-294](file:///d:/Aulia%20Project/benangbaju/src/modules/orders/order.repository.ts#L263-L294) | 🟠 High | Inventory / Return Flow | **Persetujuan retur tidak mengembalikan stok produk.** Saat admin menyetujui (`status: 'approved'`) atau menyelesaikan (`status: 'completed'`) pengajuan retur pesanan di `adminUpdateReturnRequest`, kuantitas produk yang diretur tidak ditambahkan kembali ke `product_variants.stock`. | Stok produk yang dikembalikan pembeli hilang dari inventaris (stok fisik tidak sesuai dengan stok DB). | Tambahkan increment stok varian pada `product_variants` saat retur disetujui/diselesaikan. |
| 25 | [flash-sale.repository.ts:298-305](file:///d:/Aulia%20Project/benangbaju/src/modules/flash-sales/flash-sale.repository.ts#L298-L305) | 🟠 High | Data Integrity / Storage | **Hapus banner flash sale dari storage dilakukan sebelum hapus record DB.** `adminDeleteFlashSale` menghapus gambar banner di Supabase Storage sebelum mengeksekusi `.delete().eq('id', saleId)` di DB. Jika DB delete gagal (misal terkena constraint error), banner di Storage sudah terhapus fisik. | Gambar banner flash sale terhapus permanen dari storage sementara record flash sale masih ada di DB (broken image). | Lakukan DB delete terlebih dahulu; hapus fisik gambar di Supabase Storage hanya jika DB delete berhasil. |
| 26 | [checkout/page.tsx:120-123](file:///d:/Aulia%20Project/benangbaju/src/app/%28customer%29/checkout/page.tsx#L120-L123) | 🟠 High | Voucher / Validation | **Query voucher checkout tidak memfilter `starts_at` dan `usage_limit`.** Query `checkout-available-vouchers` hanya memfilter `is_active`, `is_hidden`, dan `expires_at`. Voucher yang `starts_at`-nya belum tiba atau `used_count >= usage_limit` (kuota habis) tetap diambil dari DB. | Voucher yang belum berlaku atau sudah habis kuotanya tetap muncul di opsi "Voucher Tersedia" di halaman checkout. | Tambahkan filter `.lte('starts_at', now)` dan validasi kuota `used_count` pada query ketersediaan voucher. |
| 27 | [proxy.ts:91-103](file:///d:/Aulia%20Project/benangbaju/src/proxy.ts#L91-L103) | 🟠 High | Security / DoS Vector | **Rate limiting RPC dieksekusi sebelum autentikasi API Key.** Pada route M2M `/api/v1/inventory/sync`, `checkRateLimit` dijalankan sebelum validasi `x-api-key` di L103. | Attacker tanpa API Key bisa melakukan request spam untuk **menghabiskan kuota rate-limit** sehingga sync ERP resmi ditolak (DoS). | Pindahkan pengecekan M2M API Key (`timingSafeEqual`) sebelum `checkRateLimit` pada endpoint `/api/v1/inventory/sync`. |
| 28 | [format.ts:33-45](file:///d:/Aulia%20Project/benangbaju/src/lib/utils/format.ts#L33-L45) | 🟠 High | Timezone Drift / Admin | **`formatLocalISO` menggeser offset timezone setiap kali edit.** Fungsi ini menggunakan method lokal (`getHours()`, `getDate()`) untuk mengubah ISO string UTC dari DB menjadi string `YYYY-MM-DDTHH:MM`. Saat form disimpan kembali, string tanpa offset di-parse sebagai UTC oleh PostgreSQL. | Jam tayang Flash Sale dan Banner **tergeser +7 jam (WIB)** setiap kali admin membuka dan menyimpan form edit di dashboard admin. | Gunakan penanganan timezone yang konsisten (misal selalu format dalam ISO UTC atau include offset `+07:00`). |
| 29 | [auth/callback/route.ts:14-30](file:///d:/Aulia%20Project/benangbaju/src/app/auth/callback/route.ts#L14-L30) | 🟠 High | Auth / Security Flow | **Callback OAuth mengabaikan ketiadaan parameter `code`.** Jika `/auth/callback` dipanggil tanpa query `code`, `if (code)` dilewati dan baris 30 langsung mengembalikan `NextResponse.redirect` ke URL tujuan seolah-olah otentikasi berhasil. | Potensi bypass atau kebingungan alur autentikasi jika user mengakses endpoint callback secara langsung. | Tambahkan penanganan jika `code` tidak ada: redirect ke `/masuk?error=missing_code`. |
| 30 | [adminCustomer.repository.ts:43-52](file:///d:/Aulia%20Project/benangbaju/src/modules/users/adminCustomer.repository.ts#L43-L52) | 🟠 High | Keamanan / Auth | **Menonaktifkan pelanggan/staf tidak me-revoke token Supabase Auth.** `adminToggleCustomerStatus` dan `adminDeleteStaff` hanya mengubah `profiles.is_active = false` tanpa memanggil Supabase Auth Admin API (`auth.admin.updateUserById(id, { ban_duration: '876000h' })`). | **User yang telah di-nonaktifkan/di-ban tetap bisa bertransaksi atau mengakses DB via Supabase client RLS** menggunakan JWT session token yang masih aktif. | Panggil `supabaseAdmin.auth.admin.updateUserById(id, { ban_duration: isActive ? 'none' : '876000h' })` saat mengubah status aktif user. |
| 31 | [05_ledger_hotfix.sql:29](file:///d:/Aulia%20Project/benangbaju/sql_scripts/05_ledger_hotfix.sql#L29) | 🟠 High | DB Security / RLS | **`cleanup_checkout_locks()` RPC kehilangan `SECURITY DEFINER`.** Tanpa `SECURITY DEFINER`, fungsi RPC berjalan dengan hak akses caller (`auth.uid()`). Karena RLS aktif pada `checkout_locks`, query `DELETE FROM checkout_locks WHERE created_at < NOW() - 5 min` hanya menghapus lock milik caller itu sendiri. | Checkout lock basi milik user lain yang menggantung tidak bisa dibersihkan, berpotensi memblokir checkout user tersebut. | Tambahkan `SECURITY DEFINER` pada deklarasi fungsi RPC Postgres. |
| 32 | [banner.repository.ts:24-25](file:///d:/Aulia%20Project/benangbaju/src/modules/banners/banner.repository.ts#L24-L25) | 🟠 High | DB / Syntax Error | **Sintaks double-quote salah pada filter PostgREST `.or()` banner & koleksi.** L24-25 (`banner.repository.ts`) dan L23-24 (`collection.repository.ts`) menggunakan `.or(\`starts_at.is.null,starts_at.lte."${now}"\`)`. Tanda petik ganda di dalam value PostgREST `.or()` filter menyebabkan query gagal di-parse oleh Supabase PostgREST. | Query banner aktif dan koleksi aktif gagal/mengembalikan error di runtime. | Hapus tanda petik ganda dari value timestamp: `.or(\`starts_at.is.null,starts_at.lte.${now}\`)`. |
| 33 | [admin-product.service.ts:156-163](file:///d:/Aulia%20Project/benangbaju/src/modules/products/admin-product.service.ts#L156-L163) | 🟠 High | Data Integrity / Storage | **Hapus gambar dari storage dilakukan sebelum hapus record di DB.** Pada `deleteProduct`, `deleteImageByUrl` mengeksekusi penghapusan gambar di Supabase Storage sebelum `productRepository.adminDelete(productId)`. Jika `adminDelete` di DB gagal (misal terkena foreign key constraint dari riwayat pesanan), gambar di Storage sudah terhapus fisik. | Gambar produk terhapus permanen dari storage sementara record produk masih ada di DB (gambar patah/broken image di situs). | Lakukan penghapusan record database terlebih dahulu; hapus fisik gambar di Supabase Storage hanya jika DB delete berhasil. |
| 34 | [collection.repository.ts:168-189](file:///d:/Aulia%20Project/benangbaju/src/modules/collections/collection.repository.ts#L168-L189) | 🟠 High | Data Integrity / DB | **Update relasi produk koleksi non-transaksional.** Pada `adminUpdateCollection`, junction `collection_products` lama di-delete seluruhnya terlebih dahulu. Jika insert produk baru di L184 gagal, tidak ada rollback. | Seluruh produk terlepas dari koleksi (koleksi menjadi kosong) jika terjadi kesalahan kecil saat mengasosiasikan produk. | Bungkus operasi delete + insert junction dalam RPC Postgres (transaction) atau buat rollback mechanism. |
| 35 | [check-payment-status/index.ts:96-101](file:///d:/Aulia%20Project/supabase/functions/check-payment-status/index.ts#L96-L101) | 🟠 High | DOKU / Data Integrity | **`check-payment-status` update status tanpa guard.** Fungsi ini langsung update `orders.status` ke `processing` tanpa: (1) verifikasi amount, (2) cek apakah order sudah di-cancel, (3) log ke `payment_logs`. | Order yang sudah cancelled bisa kembali menjadi `processing`; tidak ada audit trail. | Tambahkan guard skip update jika order di terminal state (`cancelled`, `completed`). |
| 36 | [doku-webhook/index.ts:161](file:///d:/Aulia%20Project/supabase/functions/doku-webhook/index.ts#L161) | 🟠 High | DB Schema Debt | **Kolom `midtrans_order_id` digunakan untuk DOKU padahal Midtrans sudah di-deprecated.** `doku-webhook` menyimpan DOKU invoice number pada kolom `midtrans_order_id`. | Schema debt & nama kolom yang menyesatkan developer baru. | Rename kolom `midtrans_order_id` menjadi `gateway_order_id` atau `doku_invoice_number` melalui migrasi database. |
| 37 | [order.repository.ts:57-59](file:///d:/Aulia%20Project/benangbaju/src/modules/orders/order.repository.ts#L57-L59) | 🟠 High | Logic / DB | **`voucherCode` & `courierName` dikirim sebagai `undefined` ke RPC.** `params.voucherCode \|\| undefined` membuat parameter ter-omit di Supabase RPC call. | PostgreSQL RPC bisa gagal mengenali parameter atau menggunakan default yang salah. | Gunakan `?? null` alih-alih `\|\| undefined`: `p_voucher_code: params.voucherCode ?? null`. |
| 38 | [cartStore.ts:165](file:///d:/Aulia%20Project/benangbaju/src/modules/cart/stores/cartStore.ts#L165) | 🟠 High | Concurrency / State | **Race condition di Zustand cart sync menghapus item baru.** Saat `syncCart` berjalan async, jika user menambah item baru via `addItem`, `set({ items: res.data })` di L165 menimpa `items` lokal dengan response lama dari server sebelum perulangan berikutnya membaca `get().items`. | Item yang ditambah user saat sync berlangsung akan hilang dari UI dan state. | Gabungkan (merge) state lokal terbaru dengan response server atau simpan snapshot mutation request. |
| 39 | [product.repository.ts:128](file:///d:/Aulia%20Project/benangbaju/src/modules/products/product.repository.ts#L128) | 🟠 High | API / Error Handling | **`findOneBySlug` memakai `.single()` alih-alih `.maybeSingle()`.** Ketika slug tidak ditemukan (404), PostgREST melempar error `PGRST116`, yang di-catch oleh service dan dikembalikan sebagai API error 500 (`INTERNAL_ERROR`). | Halaman produk yang tidak ada mengembalikan response status 500 alih-alih 404 Not Found. | Ganti `.single()` dengan `.maybeSingle()` pada L128. |
| 40 | [shipping.repository.ts:61-69](file:///d:/Aulia%20Project/benangbaju/src/modules/shipping/shipping.repository.ts#L61-L69) | 🟠 High | Logic / DB | **`setDefaultAddress` tidak menghapus flag `is_default` alamat lama.** Fungsi ini hanya mengeset `is_default: true` pada alamat target tanpa mengeset `is_default: false` pada alamat lainnya milik user tersebut. | User dapat memiliki beberapa alamat yang semuanya berstatus `is_default = true`. | Lakukan update dua langkah (set semua `is_default = false` untuk `userId`, lalu set `is_default = true` pada `addressId`) atau gunakan RPC/trigger. |
| 41 | [proxy.ts:71](file:///d:/Aulia%20Project/benangbaju/src/proxy.ts#L71) | 🟠 High | Keamanan | **Rate limiting berbasis `x-forwarded-for` header yang bisa di-spoof.** Attacker bisa mengirim header `x-forwarded-for` palsu untuk bypass rate limiting sepenuhnya. | Rate limiting pada auth routes dan API v1 bisa di-bypass. | Gunakan header terpercaya dari edge platform seperti `x-real-ip` (Vercel). |
| 42 | [generate-payment/index.ts:9](file:///d:/Aulia%20Project/supabase/functions/generate-payment/index.ts#L9) | 🟠 High | Keamanan / CORS | **CORS `Access-Control-Allow-Origin: *` pada Edge Function pembayaran.** Memungkinkan website mana pun memanggil endpoint pembayaran ini. | Memperluas attack surface untuk cross-origin attacks. | Batasi origin ke domain resmi aplikasi (`NEXT_PUBLIC_APP_URL`). |
| 43 | [products/actions.ts:124-128](file:///d:/Aulia%20Project/benangbaju/src/modules/products/actions.ts#L124-L128) | 🟡 Medium | Error Handling / Action Result | **Respons sukses palsu pada `toggleStockNotificationAction` saat query DB gagal.** Action tidak memverifikasi properti `{ error }` dari hasil `supabase.from('stock_notifications').insert(...)` atau `.delete(...)`. | Jika DB insert/delete gagal (misal FK constraint violation), UI tetap me-render toast status sukses `"Notifikasi restock diaktifkan"`. | Tangkap `{ error }` dari Supabase query dan kembalikan `success: false` jika `error` truthy. |
| 44 | [search/page.tsx:46-52](file:///d:/Aulia%20Project/benangbaju/src/app/%28customer%29/search/page.tsx#L46-L52) | 🟡 Medium | React / Logging Trigger | **Trigger log pencarian duplikat pada re-render halaman pencarian.** `dataRes` disertakan ke dalam dependensi `useEffect`. | Setiap kali komponen me-render ulang atau TanStack Query me-refetch di background, `logSearchAction` ter-trigger berulang kali, mengotori tabel `search_logs` dengan data duplikat. | Hapus `dataRes` dari array dependensi `useEffect` atau gunakan ref flag untuk mencatat log 1x per query kata kunci. |
| 45 | [collection.repository.ts:145-165](file:///d:/Aulia%20Project/benangbaju/src/modules/collections/collection.repository.ts#L145-L165) | 🟡 Medium | Storage Leak | **`adminUpdateCollection` tidak menghapus file gambar koleksi lama saat di-replace.** Ketika admin mengunggah gambar baru untuk koleksi, gambar lama di Supabase Storage tidak di-delete. | Menimbulkan penumpukan file sampah (orphaned files) di bucket storage. | Hapus file gambar lama dari Storage jika `image_url` berubah saat update. |
| 46 | [voucher.repository.ts:98-100](file:///d:/Aulia%20Project/benangbaju/src/modules/vouchers/voucher.repository.ts#L98-L100) | 🟡 Medium | Error Handling / DB Constraint | **Unhandled constraint `23505` pada pembuatan kode voucher duplikat.** `adminCreateVoucher` me-throw error DB mentah saat admin memasukkan kode voucher yang sudah ada. | Admin menerima toast status 500 Internal Error (`Gagal membuat voucher`) alih-alih pesan validasi 400 `"Kode voucher sudah digunakan"`. | Tangkap error `23505` dan kembalikan `fail(ApiErrorCode.VALIDATION_ERROR, 'Kode voucher sudah digunakan')`. |
| 47 | [voucher.service.ts:32-53](file:///d:/Aulia%20Project/benangbaju/src/modules/vouchers/voucher.service.ts#L32-L53) | 🟡 Medium | Validation / Business Logic | **Ketiadaan validasi rentang tanggal (`starts_at < expires_at`) pembuatan voucher.** `adminCreateVoucher` dan `adminUpdateVoucher` tidak mengecek tanggal mulai vs tanggal berakhir. | Admin dapat menyimpan voucher dengan `starts_at` lebih besar daripada `expires_at`, membuat voucher menjadi tidak bisa digunakan selamanya. | Tambahkan validasi `if (new Date(starts_at) >= new Date(expires_at)) throw new ValidationError('Tanggal mulai harus sebelum tanggal berakhir')`. |
| 48 | [admin-product.service.ts:108-133](file:///d:/Aulia%20Project/benangbaju/src/modules/products/admin-product.service.ts#L108-L133) | 🟡 Medium | Storage Leak | **File gambar produk yang dihapus saat edit tidak dibersihkan dari Storage.** `updateProduct` mengirim `imageIdsToDelete` ke DB RPC untuk menghapus row database, tetapi tidak pernah memanggil `deleteImageByUrl` untuk file fisiknya. | Gambar produk yang di-delete/ganti oleh admin pada form edit tertahan selamanya sebagai unreferenced orphan files di Storage bucket. | Ambil URL dari `imageIdsToDelete` dan panggil `deleteImageByUrl` untuk menghapus file fisik di Supabase Storage. |
| 49 | [admin-product.service.ts:148](file:///d:/Aulia%20Project/benangbaju/src/modules/products/admin-product.service.ts#L148) | 🟡 Medium | API Contract / Error Handling | **String error mentah sebagai parameter `ApiErrorCode` pada update product.** Catch block `updateProduct` memanggil `fail('Gagal memperbarui produk', ...)` dengan parameter `code` berupa string bebas. | Response API mengembalikan `error.code = "Gagal memperbarui produk"` yang melanggar kontrak tipe data API standard. | Gunakan enum valid: `fail(ApiErrorCode.INTERNAL_ERROR, error.message \|\| 'Transaction failed')`. |
| 50 | [storage.ts:55-61](file:///d:/Aulia%20Project/benangbaju/src/lib/supabase/storage.ts#L55-L61) | 🟡 Medium | Storage / Path Truncation | **`deleteImageByUrl` membuang path subfolder storage bucket.** `deleteImageByUrl` mengambil nama file hanya dari segmen URL terakhir (`urlParts[urlParts.length - 1]`). | Menghapus gambar yang tersimpan di subfolder (misal `bucket/banners/2026/summer.png`) gagal secara diam-diam karena path `2026/` terbuang. | Ekstrak path relatif lengkap terhadap nama bucket alih-alih hanya segmen file terakhir. |
| 51 | [Select.tsx:26-295](file:///d:/Aulia%20Project/benangbaju/src/shared/components/Select.tsx#L26-L295) | 🟡 Medium | Component / Form Data | **Komponen UI `Select` tidak memiliki elemen `<select>` / `<input>` tersembunyi.** Komponen menerima ref `HTMLSelectElement` tetapi tidak merender elemen form HTML aktual di DOM. | Penggunaan `Select` di dalam `<form action={...}>` standar membuat **nilai terpilih tidak terkirim di `FormData`** saat submit. | Rendernya hidden `<select>` atau `<input name={name} value={value} />` di dalam komponen `Select`. |
| 52 | [shipping.repository.ts:23-32](file:///d:/Aulia%20Project/benangbaju/src/modules/shipping/shipping.repository.ts#L23-L32) | 🟡 Medium | Address / State | **Duplikat flag `is_default` saat menambah alamat baru.** `addUserAddress` tidak meng-unset `is_default: false` pada alamat eksisting saat pengguna meng-input alamat baru dengan `is_default = true`. | Pengguna memiliki beberapa alamat yang semuanya berstatus `is_default = true`. | Unset `is_default` seluruh alamat pengguna sebelum me-insert alamat baru ber-flag `is_default = true`. |
| 53 | [shipping.repository.ts:51-59](file:///d:/Aulia%20Project/benangbaju/src/modules/shipping/shipping.repository.ts#L51-L59) | 🟡 Medium | Address / UX | **Status alamat default hilang saat menghapus alamat default utama.** `deleteUserAddress` tidak mempromosikan salah satu alamat tersisa menjadi alamat default baru saat alamat ber-flag `is_default = true` dihapus. | Semua alamat tersisa berstatus `is_default = false`, membuat auto-select alamat default pada checkout menjadi `undefined`. | Promosikan alamat terbaru yang tersisa menjadi `is_default = true` jika alamat default dihapus. |
| 54 | [flash-sale.repository.ts:221-230](file:///d:/Aulia%20Project/benangbaju/src/modules/flash-sales/flash-sale.repository.ts#L221-L230) | 🟡 Medium | Storage Leak | **`adminUpdateFlashSale` tidak menghapus file banner lama saat di-replace.** Ketika admin mengunggah banner baru untuk flash sale, banner lama di Supabase Storage tidak di-delete. | Menimbulkan akumulasi file sampah (orphaned unreferenced files) di Storage bucket. | Hapus file gambar lama dari Storage jika `banner_url` berubah saat update. |
| 55 | [category.repository.ts:100-124](file:///d:/Aulia%20Project/benangbaju/src/modules/categories/category.repository.ts#L100-L124) | 🟡 Medium | Storage Leak | **`adminUpdateCategory` tidak menghapus file gambar lama saat di-replace.** Ketika admin mengunggah gambar baru untuk kategori, gambar lama di Supabase Storage tidak di-delete. | Menimbulkan akumulasi file sampah (orphaned unreferenced files) di Storage bucket. | Hapus file gambar lama dari Storage jika `image_url` berubah saat update. |
| 56 | [shipping.utils.ts:36](file:///d:/Aulia%20Project/benangbaju/src/modules/shipping/shipping.utils.ts#L36) | 🟡 Medium | Shipping / Calculation | **Kalkulasi berat `calculateCartWeight` menganggap produk 0-gram berbobot 200g.** `Number(weight) \|\| DEFAULT_WEIGHT_GRAM` mengevaluasi `0 \|\| 200` menjadi 200. | Produk 0-gram (hadiah/digital promo) secara keliru dihitung berbobot 200g pada total ongkir checkout. | Gunakan nullish coalescing: `Number(weight) ?? DEFAULT_WEIGHT_GRAM`. |
| 57 | [voucher.repository.ts:157-164](file:///d:/Aulia%20Project/benangbaju/src/modules/vouchers/voucher.repository.ts#L157-L164) | 🟡 Medium | Error Handling / FK Constraint | **Unhandled constraint error `23503` saat menghapus voucher terpakai.** `adminDeleteVoucher` me-throw error DB mentah ketika voucher yang akan dihapus pernah digunakan pada pesanan pelanggan. | Admin menerima error 500 Internal Error alih-alih pesan validasi 400 (`"Voucher telah digunakan pada riwayat pesanan"`). | Tangkap error `23503` dan kembalikan pesan validasi yang ramah pengguna. |
| 58 | [route.ts:31](file:///d:/Aulia%20Project/benangbaju/src/app/api/v1/inventory/sync/route.ts#L31) | 🟡 Medium | Input Validation | **Tanpa validasi nilai stok non-negatif & integer pada API sync ERP.** Validasi hanya mengecek `typeof stock === 'number'`, mengizinkan angka negatif (`-10`) atau `NaN`. | Stok bernilai negatif merusak logika checkout; `NaN` melempar constraint error di DB. | Tambahkan validasi: `if (!Number.isInteger(update.stock) \|\| update.stock < 0) return 400`. |
| 59 | [DataTable.tsx:122](file:///d:/Aulia%20Project/benangbaju/src/shared/components/DataTable.tsx#L122) | 🟡 Medium | React / Rendering | **React key evaluation bug saat `row.id` bernilai `0`.** `DataTable` menggunakan `key={row.id ?? rIdx}`. Untuk `row.id = 0`, `0 ?? rIdx` menghasilkan key `0` untuk seluruh baris ber-ID 0. | Duplicate key warning di React Virtual DOM saat menyorot atau me-render baris tabel. | Gunakan `row.id !== undefined && row.id !== null ? row.id : rIdx`. |
| 60 | [DataTable.tsx:139](file:///d:/Aulia%20Project/benangbaju/src/shared/components/DataTable.tsx#L139) | 🟡 Medium | Component / Lookup | **Property lookup gagal untuk nested key (misal `profiles.name`).** Akses langsung `row["profiles.name"]` me-return `undefined` jika `col.render` tidak ditentukan. | Sel tabel tampil kosong untuk data ter-join dari Supabase. | Implementasikan fungsi pembaca nested object path (seperti `lodash.get`). |
| 61 | [banner.repository.ts:95-124](file:///d:/Aulia%20Project/benangbaju/src/modules/banners/banner.repository.ts#L95-L124) | 🟡 Medium | Storage Leak | **`adminUpdateBanner` tidak menghapus file gambar lama saat di-replace.** Ketika admin mengunggah gambar baru untuk banner, gambar lama di Supabase Storage tidak di-delete. | Menimbulkan akumulasi file sampah (orphaned unreferenced files) di Storage bucket. | Hapus file gambar lama dari Storage jika `image_url` atau `image_mobile_url` berubah saat update. |
| 62 | [product.repository.ts:41](file:///d:/Aulia%20Project/benangbaju/src/modules/products/product.repository.ts#L41) | 🟡 Medium | Performance Overhead | **Query `findMany` membaca seluruh tabel `categories` ke memori Node.js.** `findMany` mengeksekusi `select('id, slug, parent_id')` tanpa limit untuk memfilter `categorySlug` di JS memory. | Membebankan memori server & latensi database berlebihan pada setiap filter katalog. | Gunakan query join `categories!inner(slug)` langsung pada query `products`. |
| 63 | [admin-log.repository.ts:73-80](file:///d:/Aulia%20Project/benangbaju/src/modules/admin-logs/admin-log.repository.ts#L73-L80) | 🟡 Medium | Join Edge Case | **Join profiles log aktivitas bernilai null jika staf dihapus.** Jika staf pembuat log aktivitas telah dihapus dari database, `profiles` me-return `null` tanpa nama fallback. | Kolom 'Dilakukan Oleh' pada tabel log aktivitas admin di dashboard tampil kosong. | Berikan fallback name: `profiles = { name: rawProfiles?.name \|\| 'Staf Terhapus', email: rawProfiles?.email \|\| null }`. |
| 64 | [category.repository.ts:74,110](file:///d:/Aulia%20Project/benangbaju/src/modules/categories/category.repository.ts#L74) | 🟡 Medium | Validation / Error Handling | **Unhandled unique constraint `slug` pada kategori me-return status 500.** `adminCreateCategory` dan `adminUpdateCategory` tidak mengecek ketersediaan `slug` sebelum query DB. | Admin melihat toast error 500 Internal Error saat memasukkan slug yang sudah ada. | Tangkap error `23505` (unique_violation) dan kembalikan `fail(ApiErrorCode.VALIDATION_ERROR, 'Slug kategori sudah digunakan')`. |
| 65 | [analytics.ts:46](file:///d:/Aulia%20Project/benangbaju/src/modules/analytics/actions/analytics.ts#L46) | 🟡 Medium | Reporting / Analytics | **Query analitik voucher hanya menghitung order bernilai `completed`.** `getAdminAnalyticsAction` memfilter order dengan `.eq('status', 'completed')`. Pesanan aktif yang sedang diproses (`processing`) atau dikirim (`shipped`) diabaikan total. | Data statistik nilai diskon dan penggunaan voucher di Dashboard Analitik Admin di bawah nilai aktual (under-reported). | Sertakan status `processing`, `shipped`, dan `completed` di filter query: `.in('status', ['processing', 'shipped', 'completed'])`. |
| 66 | [review.service.ts:43](file:///d:/Aulia%20Project/benangbaju/src/modules/reviews/review.service.ts#L43) | 🟡 Medium | Auth / Security | **`voteHelpful` tidak memerlukan autentikasi user.** Service dan repository menjalankan `voteHelpful` tanpa memverifikasi session user ID (`userId`). | Bot anonim dapat memicu request upvote ulasan produk secara otomatis dan masif. | Lindungi action `voteHelpful` dengan `requireAuth()` dan batasi 1 vote per user per ulasan. |
| 67 | [admin-order.service.ts:18-26](file:///d:/Aulia%20Project/benangbaju/src/modules/orders/admin-order.service.ts#L18-L26) | 🟡 Medium | Search Escaping | **Over-escaping tanda koma dan kurung pada keyword pencarian admin.** `admin-order.service.ts` meng-escape `,` `(` `)` menjadi `\,` `\(`. Saat dikirim ke PostgREST `.ilike`, PostgREST mencari karakter backslash literal. | Pencarian pesanan admin yang mengandung nomor HP/tanda baca mengembalikan hasil kosong. | Hanya escape karakter khusus PostgREST LIKE (`%` dan `_`). |
| 68 | [voucher.repository.ts:76,117](file:///d:/Aulia%20Project/benangbaju/src/modules/vouchers/voucher.repository.ts#L76) | 🟡 Medium | Input Sanitization | **Pembuatan & update kode voucher tidak membersihkan spasi/case.** `adminCreateVoucher` dan `adminUpdateVoucher` menyimpan `code` tanpa `.trim().toUpperCase()`. Input spasi tidak sengaja (misal `"DISKON10 "`) dari admin menyebabkan query RPC `validate_voucher` gagal mencocokkan kode saat customer mengetik `"DISKON10"`. | Voucher resmi gagal digunakan oleh pelanggan akibat spasi/case mismatch. | Bersihkan input kode voucher sebelum disimpan: `code: voucherData.code.trim().toUpperCase()`. |
| 69 | [checkout/page.tsx:167](file:///d:/Aulia%20Project/benangbaju/src/app/%28customer%29/checkout/page.tsx#L167) | 🟡 Medium | Performance / UX | **`totalWeight = 0` saat loading memicu query ongkir awal yang salah.** Saat `variantDetails` belum selesai di-fetch, `totalWeight` bernilai 0, memicu `useShippingRates(zone_id, 0)` yang berpotensi gagal atau berjalan 2 kali. | Terjadi query ongkos kirim ganda saat halaman checkout pertama kali dimuat. | Aktifkan query `useShippingRates` hanya saat `totalWeight > 0` dan `variantDetails` sudah tersedia. |
| 70 | [order.service.ts:95](file:///d:/Aulia%20Project/benangbaju/src/modules/orders/order.service.ts#L95) | 🟡 Medium | API Contract | **`createOrder` mengubah error validasi bisnis menjadi status 500.** Saat `orderRepository.create` melempar error (misal stok tidak mencukupi atau voucher tidak berlaku), catch block di L95 mengembalikan `fail(ApiErrorCode.INTERNAL_ERROR, ...)`. | Client/UI menerima status 500 Internal Error alih-alih pesan validasi bisnis yang ramah pengguna. | Kembalikan `fail(ApiErrorCode.VALIDATION_ERROR, error.message)`. |
| 71 | [daftar/page.tsx:51](file:///d:/Aulia%20Project/benangbaju/src/app/%28auth%29/daftar/page.tsx#L51) | 🟡 Medium | Auth / UX | **Inkonsistensi validasi panjang minimum password.** Halaman daftar menuntut min 8 karakter, reset password menuntut min 6 karakter (`reset-password/page.tsx:57`), dan pembuat staf admin menuntut min 12 karakter (`users/actions.ts:40`). | Pengguna yang mendaftar dengan 6 karakter lewat reset password tidak bisa mendaftar ulang di form registrasi reguler. | Samakan standar validasi password aplikasi (minimal 8 karakter di semua form). |
| 72 | [inventory.repository.ts:26-47](file:///d:/Aulia%20Project/benangbaju/src/modules/products/inventory.repository.ts#L26-L47) | 🟡 Medium | Performa / N+1 DB | **N+1 sequential DB queries pada pengiriman notifikasi restock.** `checkAndDispatchStockNotifications` mengeksekusi 2 query per pelanggan (insert notification + update stock_notification) secara berurutan di dalam perulangan `for`. | Restock barang populer yang diikuti 100+ user akan memicu 200+ query DB berurutan, menyebabkan request timeout / API 504. | Lakukan bulk insert ke `notifications` dan bulk update ke `stock_notifications` dalam 2 query gabungan. |
| 73 | [wishlistStore.ts:48-50](file:///d:/Aulia%20Project/benangbaju/src/modules/products/stores/wishlistStore.ts#L48-L50) | 🟡 Medium | State / UX | **Tanpa rollback optimistic state saat toggle wishlist DB gagal.** Di `toggleWishlist`, `set({ productIds: updatedIds })` meng-update UI secara instan. Jika query Supabase DB di L36-46 melempar error, error hanya di-log ke console tanpa mengembalikan `productIds` ke state semula. | UI menampilkan status wishlist yang tidak akurat (misal icon hati merah padahal gagal tersimpan di DB). | Simpan state `productIds` lama sebelum update, dan lakukan `set({ productIds: previousIds })` jika terjadi error di block `catch`. |
| 74 | [midtrans-webhook/index.ts](file:///d:/Aulia%20Project/supabase/functions/midtrans-webhook/index.ts) | 🟡 Medium | Clean Code / Security | **Sisa Edge Function `midtrans-webhook` masih ter-deploy padahal Midtrans sudah tidak dipakai.** Folder `supabase/functions/midtrans-webhook` beserta konfigurasi env var terkait Midtrans masih ada di codebase. | Memperbesar attack surface, membingungkan maintenance, dan menambah akumulasi dead code. | Hapus folder `midtrans-webhook` dari `supabase/functions` dan bersihkan dokumentasi/env variables Midtrans. |
| 75 | [SupabaseProvider.tsx:71-77](file:///d:/Aulia%20Project/benangbaju/src/shared/providers/SupabaseProvider.tsx#L71-L77) | 🟡 Medium | Concurrency / Auth | **Unqueued async callback pada Supabase `onAuthStateChange`.** Handler `onAuthStateChange` menjalankan async function tanpa antrean. Event berturut-turut (misal `SIGNED_OUT` lalu `SIGNED_IN`) memicu `handleUserSession` secara paralel. | Race condition pada auth state dan sync cart/wishlist ganda saat login/logout cepat. | Gunakan ref flag atau antrean untuk memastikan hanya handler sesi terbaru yang diproses. |
| 76 | [OrderDetailClient.tsx:72](file:///d:/Aulia%20Project/benangbaju/src/app/%28customer%29/pesanan/%5BorderNumber%5D/OrderDetailClient.tsx#L72) | 🟡 Medium | Logic | **Re-order menggunakan hardcoded stock `99`.** Tombol "Beli Lagi" mengeset stok item ke 99 tanpa memeriksa stok aktual produk di database. | User bisa checkout melebihi stok aktual. | Fetch stok aktual dari database sebelum memasukkan ke keranjang. |
| 77 | [shipping.repository.ts:86](file:///d:/Aulia%20Project/benangbaju/src/modules/shipping/shipping.repository.ts#L86) | 🟡 Medium | DB / Syntax Error | **Sintaks double-quote salah pada `searchDistricts` PostgREST filter.** L86 me-render `.or(\`district_name.ilike."${formattedQuery}",city_name.ilike."${formattedQuery}"\`)`. | Pencarian kecamatan di modal alamat bisa gagal/error. | Hapus tanda petik ganda: `.or(\`district_name.ilike.${formattedQuery},city_name.ilike.${formattedQuery}\`)`. |
| 78 | [review.repository.ts:287-300](file:///d:/Aulia%20Project/benangbaju/src/modules/reviews/review.repository.ts#L287-L300) | 🟡 Medium | DB / Partial Failures | **Gagal insert media meninggalkan ulasan tanpa media (non-atomic).** Pada `customerSubmitReview`, parent `product_reviews` diinsert terlebih dahulu. Jika insert ke `review_media` di L295 gagal, fungsi melempar error tapi row ulasan sudah terlanjur tersimpan di DB. | Ulasan tersimpan setengah (tanpa gambar), dan user tidak bisa mencoba lagi karena duplicate constraint pada order item. | Bungkus dalam Supabase RPC transaction atau hapus (rollback) row `product_reviews` jika insert media gagal. |
| 79 | [adminStaff.repository.ts:68](file:///d:/Aulia%20Project/benangbaju/src/modules/users/adminStaff.repository.ts#L68) | 🟡 Medium | Anti-pattern / Async | **`setTimeout(500)` di serverless environment saat membuat staf.** `await new Promise(r => setTimeout(r, 500))` digunakan untuk menunggu database trigger membuat profil. | Menambah delay 500ms pada response, dan rentan race condition jika trigger butuh waktu > 500ms. | Hilangkan `setTimeout`, gunakan `upsert` langsung dengan handling yang tepat. |
| 80 | [middleware.ts:7](file:///d:/Aulia%20Project/benangbaju/src/lib/supabase/middleware.ts#L7) | 🟡 Medium | Performa / Cache | **In-memory redirect cache tidak ter-share di serverless.** Cache `Map` hanya hidup per instance Node.js. Di serverless (Vercel), hit rate sangat rendah. | Latency tambahan ~50-100ms per request karena me-hit DB `redirects`. | Manfaatkan `unstable_cache` atau `cacheTag/cacheLife`. |
| 81 | [checkout/page.tsx:109](file:///d:/Aulia%20Project/benangbaju/src/app/%28customer%29/checkout/page.tsx#L109) | 🟡 Medium | React | **`useMemo` dependency yang di-suppress.** `addresses` bernilai `addressesRes?.data \|\| []` (referensi array baru setiap render), menyebabkan `useEffect` re-run terus-menerus. | Re-render tidak perlu & address selection bisa ke-reset. | Wrap `addresses` dengan `useMemo`: `const addresses = useMemo(() => addressesRes?.data ?? [], [addressesRes?.data])`. |
| 82 | [OrderDetailClient.tsx:159](file:///d:/Aulia%20Project/benangbaju/src/app/%28customer%29/pesanan/%5BorderNumber%5D/OrderDetailClient.tsx#L159) | 🟡 Medium | React | **`checkPaymentMutation` di deps `useCallback` buat loop.** Object `checkPaymentMutation` berubah referensi me-render, membuat `startPaymentVerification` me-recreate. | Re-trigger efek verifikasi pembayaran. | Simpan ref untuk mutation function. |
| 83 | [order.repository.ts:173-177](file:///d:/Aulia%20Project/benangbaju/src/modules/orders/order.repository.ts#L173-L177) | 🟡 Medium | Keamanan | **PostgREST filter injection di `adminFindMany`.** Karakter `.` (dot) tidak di-escape di pencarian admin, bisa dipakai untuk manipulasi sub-query PostgREST. | Potensi manipulasi filter oleh admin. | Escape karakter `.` atau gunakan RPC. |
| 84 | [generate-payment/index.ts:64](file:///d:/Aulia%20Project/supabase/functions/generate-payment/index.ts#L64) | 🟡 Medium | Logic | **`requestTimestamp` truncate tanpa offset.** Jika ada perbedaan jam antara Edge Runtime & DOKU server, signature bisa invalid. | Intermittent payment generation failure. | Pastikan format ISO8601 UTC penuh. |
| 85 | [next.config.ts:80](file:///d:/Aulia%20Project/benangbaju/next.config.ts#L80) | 🟢 Low | Keamanan | **CSP `script-src` menggunakan `'unsafe-inline'` & `'unsafe-eval'`.** Melemahkan fungsi utama Content Security Policy terhadap XSS. | CSP tidak maksimal menahan XSS injection. | Hapus `'unsafe-eval'` jika tidak dibutuhkan library DOKU/Google. |
| 86 | [constants.ts:5](file:///d:/Aulia%20Project/benangbaju/src/lib/constants.ts#L5) | 🟢 Low | Code Quality | **Status `'paid'` di `ORDER_STATUS` tidak ada di type union `Order.status`.** | Dead code yang membingungkan. | Hapus `'paid'` atau selaraskan type union `Order.status`. |
| 87 | [getImageUrl.ts:2](file:///d:/Aulia%20Project/benangbaju/src/lib/getImageUrl.ts#L2) | 🟢 Low | Konfigurasi | **Supabase storage URL hardcoded.** | Gambar patah jika Supabase project diswitch/dipindah. | Derive dari `process.env.NEXT_PUBLIC_SUPABASE_URL`. |
| 88 | [admin-order.service.ts:129-143](file:///d:/Aulia%20Project/benangbaju/src/modules/orders/admin-order.service.ts#L129-L143) | 🟢 Low | Error Handling | **Gagal kirim notifikasi membatalkan response sukses admin.** Exception di notifikasi tertangkap outer catch. | Admin melihat toast error padahal status order di DB sudah berubah. | Bungkus `sendOrderStatusNotification` dalam try-catch terpisah. |
| 89 | [cartStore.ts:147](file:///d:/Aulia%20Project/benangbaju/src/modules/cart/stores/cartStore.ts#L147) | 🟢 Low | Concurrency | **Concurrent `syncCart` waiter keluar tanpa sync.** Waiter loop di L147 keluar dan me-return tanpa mengeksekusi sync jika `isSyncing` selesai. | Sync request yang bersamaan bisa terlewati. | Panggil `syncCart` ulang setelah lock rilis jika `needsResync` bernilai true. |
| 90 | [product.repository.ts:162-187](file:///d:/Aulia%20Project/benangbaju/src/modules/products/product.repository.ts#L162-L187) | 🟢 Low | Admin Search UX | **Pencarian produk admin `adminFindMany` hanya memfilter nama.** Query hanya mengeksekusi `.ilike('name', ...)`, mengabaikan SKU varian (`product_variants.sku`) dan deskripsi. | Admin tidak dapat mencari produk berdasarkan SKU atau kode barang di katalog admin. | Sertakan `.or()` filter yang mencakup `name` dan `product_variants.sku`. |

---

## Catatan Konfirmasi & Keputusan Desain

1. **[RESOLVED] `images.unoptimized: true` di `next.config.ts`** — Dikonfirmasi pengguna bahwa ini **disengaja** karena aplikasi sudah menggunakan Cloudflare CDN Worker (`cdn.benangbaju.com`) untuk optimasi & caching gambar di Edge.
2. **[RESOLVED] Midtrans Deprecation** — Dikonfirmasi pengguna bahwa **Midtrans sudah sepenuhnya tidak digunakan lagi**. Folder `supabase/functions/midtrans-webhook` direkomendasikan untuk di-delete total, dan kolom `midtrans_order_id` direkomendasikan untuk di-rename ke `gateway_order_id` atau `doku_invoice_number`.
3. **`searchDistrictsAction` & `logSearchAction` tanpa auth / rate limit** — Perlu dipastikan apakah endpoint publik ini cukup aman dari spamming request.
