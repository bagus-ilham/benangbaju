import { ReactNode } from 'react'
import { AuthLayout } from '@/components/layout/AuthLayout'

export default function AuthGroupLayout({ children }: { children: ReactNode }) : import("d:/Aulia Project/benangbaju/node_modules/@types/react/index").JSX.Element {
  return <AuthLayout>{children}</AuthLayout>
}
